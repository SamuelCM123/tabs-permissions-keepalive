import { permissions } from './permissions.ts';
import { saveLastPath } from './saveLastPath.ts';
import { redirectTabs } from './redirectTabs.ts';

export {
    permissions,
    saveLastPath,
    redirectTabs
}